{
  "name": "breeding-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kontrol-apa/breeding-subgraph",
    "create-local": "graph create --node http://localhost:8020/ kontrol-apa/breeding-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ kontrol-apa/breeding-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kontrol-apa/breeding-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "gluegun": "^5.1.2"
  }
}
